import React from "react"

export default ({ children }) => (
  <div>
    <div style={{
        textAlign: "right",
        fontSize: "1em",
        padding: "2em"
      }}>ll mdx tests</div>
    <div>{children}</div>
  </div>
)
